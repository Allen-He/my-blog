import Vue from 'vue';
import {
  Pagination,
  Timeline,
  FormModel,
  Input,
  Button,
} from 'ant-design-vue';

Vue.use(Pagination);
Vue.use(Timeline);
Vue.use(FormModel);
Vue.use(Input);
Vue.use(Button);
